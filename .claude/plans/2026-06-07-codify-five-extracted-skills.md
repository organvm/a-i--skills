# Plan: Codify the 5 Extracted Skills (2026-06-07)

**Status:** AWAITING CONDUCTOR APPROVAL (explicit gate in continuation prompt)
**Branch:** `feat/codify-five-extracted-skills-2026-06-07` (worktree-isolated; main checkout has unrelated dirty residue)
**Source of the 5:** Gemini synthesis session `~/.gemini/tmp/4jp/chats/session-2026-06-07T15-13-8bb8f846.jsonl` (sweep of 128 sessions → 77 prompts → 5 recurring workflows). Raw extraction preserved at `~/.claude/jobs/9a448d13/tmp/all_prompts2.txt` (rescued from volatile `/tmp`).

## Provenance chain
- Handoff: `~/Code/organvm/.conductor/active-handoff.md` (S-HallMonitor 2026-06-07)
- Closeout: `~/Code/organvm/.gemini/plans/closeout-2026-06-07.md`
- Phase 0 verification: `a1913e2` reviewed + pushed (`28f4b36..482408f` via HTTPS/gh fallback); P0 credential-roster redaction `482408f`; session state containerized at `~/Code/organvm/corpvs-worktrees/session-2026-06-07` (branch `session-state-2026-06-07`, committed, push HELD — public repo, raw transcripts).

## Canonical routing (EXPLORE findings)
- **Authoring home:** `~/Code/organvm/a-i--skills/skills/<category>/<name>/SKILL.md` — the skills-registry canonical surface (reliquary `canonical-surfaces-reference.md`).
- **Registry:** `distributions/skills-registry.json` is GENERATED (`scripts/generate_registry.py`) — never hand-edited.
- **Distributions:** per-consumer renders regenerated by `scripts/refresh_skill_collections.py`.
- **Validation:** `scripts/validate_skills.py` + CI secret detection (`sk-`, `ghp_`, `AKIA`).
- **Runtime:** `~/.agents/skills/` is a deployed surface (1 chezmoi-managed skill + 177 installed); activation copy happens from `distributions/claude/skills/<name>/`, matching the existing install pattern (e.g. today's Cloudflare batch).
- **NOT the domus path:** `dot_agents/skills/` carries only the one-off `prompt-relay-feedback` per-agent activation; not the fleet pattern.

## The 5 skills (names per flat-ontological-naming: say what it does)

| # | Directory name | Category | Essence |
|---|---|---|---|
| 1 | `session-governance-audit` | tools | Transcript → annotated artifact bibliography, energy/atom tallies, Gap-vs-Vacuum classification → Session Governance Index. Triggers: "visibility-schema-substrate-sweep", "session cascade audit". |
| 2 | `corpus-persona-extraction` | data | Session JSONLs → vocabulary frequency, idioms, structural analogies → `ideal_yearning` + `archetypal_pattern` → append `{persona_id}.lexicon.yaml`. Triggers: "extract persona", "build a lexicon". |
| 3 | `recursive-boil-down` | knowledge | Source + `seed.yaml` → four-tier decomposition (Macro/Bricks/Elements/Primitives) → comparison table vs existing substrates. Triggers: "decompose into primitives", new-repo architecture analysis. |
| 4 | `prompt-atom-formalization` | data | Rough markdown/session exports → core-intent atoms (laws/values/branch-vectors) → machine-readable YAML frontmatter per RAW→CLEANED→ELEVATED doctrine. Triggers: "Prompt Atom extraction pass". |
| 5 | `natural-center-extraction` | creative | Narratological scoring (pathos/logos/ethos/kairos/density) over text → single highest-scoring contiguous block + justification. Triggers: "find the Natural Center". |

## BUILD steps (post-approval)
1. For each skill: `skills/<category>/<name>/SKILL.md` with full YAML frontmatter matching the registry entry schema (name, description w/ trigger phrases, license MIT, complexity, time_to_learn, tags, inputs, outputs, side_effects, triggers, organ_affinity) + full procedural body (no stubs).
2. Regenerate: `python3 scripts/generate_registry.py` → registry; `python3 scripts/refresh_skill_collections.py` → distributions + lockfile.
3. VERIFY: `python3 scripts/validate_skills.py`; secret-pattern scan of new files; registry diff shows exactly +5 entries; `git diff` review.
4. PR to `a-organvm/a-i--skills` (worktree→PR→merge default); push via HTTPS/gh fallback while SSH is locked.
5. Activate: copy `distributions/claude/skills/<name>/` → `~/.agents/skills/<name>/` (existing install pattern).
6. LEARN: memory entry (project artifact) + MEMORY.md pointer; close the handoff next-action; mark `.conductor/active-handoff.md` consumed.

## Deferred to conductor (not blocking this plan)
- **D1 — session-state branch push:** `session-state-2026-06-07` holds raw `session.jsonl` transcripts; corpvs is PUBLIC. Recommend: do NOT push; route transcripts to the private session-meta substrate instead.
- **D2 — credential rotation:** redaction `482408f` cleans the tip only; literals remain in public history (`e82942c` blob). Rotate the 3 AWS keys + password if not already done.
- **D3 — registry drift:** runtime `~/.agents/skills/closeout/SKILL.md` is stale vs source; a-i--skills main checkout carries 23 uncommitted files (closeout/cross-agent-handoff propagation + Cloudflare batch) — separate triage.
